div.ptl#header
  div.resz.ltrt
    div.row
      div.column
        ol.menu.dictionary#MyOrdbok-logo(data-lang=sol.id, title=title)
          each v, route in nav.dictionary
            li(class=v.route class={active: v.active})
              a(href=v.url.replace(':id',sol.name.toLowerCase()).replace('?',''))
                span #{sol.name}
                | -
                span Myanmar
      div.column.wide
        include search-form