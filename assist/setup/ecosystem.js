import { deploy } from "lethil";

/**
 * Purely for deployment from local/workspace
 * @alias deploy.ecosystem
 */
// export default const createOrUpdate = deploy.ecosystem.createOrUpdate;
export default deploy.ecosystem.createOrUpdate;
